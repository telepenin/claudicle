[Unit]
Description=Claudicle UI Server
After=network.target

[Service]
Type=simple
ExecStart={{NODE_PATH}} {{SERVER_JS}}
Restart=on-failure
RestartSec=5
EnvironmentFile={{ENV_FILE}}

[Install]
WantedBy=default.target
